{
  "name": "returnfast-customer-account",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:helpers": "tsc --project tsconfig.json --noEmit false --outDir dist --module NodeNext --moduleResolution NodeNext --rootDir src",
    "test:url": "npm run build:helpers && node src/returnPortalUrl.test.mjs"
  },
  "dependencies": {
    "@preact/signals": "^2.3.0",
    "@shopify/ui-extensions": "latest",
    "preact": "^10.10.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
