{
  "name": "returnfast-customer-account",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build:helpers": "tsc src/returnPortalUrl.ts src/runtime.ts --noEmit false --outDir dist --module NodeNext --moduleResolution NodeNext --target ES2022 --strict --skipLibCheck --types vite/client",
    "test:url": "npm run build:helpers && node src/returnPortalUrl.test.mjs"
  },
  "dependencies": {
    "@preact/signals": "^2.3.0",
    "@shopify/ui-extensions": "2026.7.0",
    "preact": "^10.10.0"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
